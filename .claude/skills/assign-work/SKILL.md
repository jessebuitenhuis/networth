---
name: assign-work
description: Use this to find pending tasks (features, stories, etc.) and assign the work to delegated agents
disable-model-invocation: true
---

Look for pending tasks in the repository.
Propose the next tasks to the user and ask for approval.
Then, delegate the individual tasks using the `/delegate` skill
