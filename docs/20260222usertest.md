- When importing a CSV, there's no margin between 'Select CSV file' label and the input. Also, it's very unclear the 'Choose file' field is a button.
- When importing a CSV the 'sample data' table does not fit in the modal. The modal should be larger, and the table should be constrainted to the model width. If it scrolls, only the table should scroll so that the inputs are visible always. This also goes for the ready for import preview dialog (step 3)
- When importing a CSV, it does not automatically recognize the transactions. This could be done in two ways: a simple mapping (for example, the field 'Datum' or 'Date' should be recognized. Same with 'Omschrijving' or 'Description'). Another option could be to use a cheap AI call with the headers.
- When importing a CSV and a date field is selected, it should recognize the date format itself. The standard should be selected based on the users locale (for me in Amsterdam it's usually dd-mm-yyyy)
- When importing a CSV, the same field can be mapped to multiple fields, that should not be allowed
- When importing a CSV, it helps to have the sample data like it is currently. It would also be nice to see a preview of how the mapping will happen with the selected options in the same screen (maybe next to the selects.
- When looking at a account detail, the column 'Account' is not usefull and can be removed. It should stay in the planning tab when multiple accounts are selected (that could be the condition, depending on the implementation)
- When looking at a graph, the 0 line should be shown always
- When looking at the planning tab, and the amount is a negative amount (for example -334), the 0 line should be centered instead of at the top of the chart. That makes it easy to see the amount is below zero.
- The graph Y-axis should use even numbers. For instance, when the amount is -338, round it down to -350 or -400.
- When looking at the planning, I want to see a section of history so I can see the changes. As a default, use one period in the past (when selected a week, show -1week to +1week)
- When looking at the planning, I want to step backwards and forwards with arrow buttons. For example, when 1W selected, pressing next arrow should move the time 1week up.
- When no scenarios exist, the scenario pickers should not be visible
- When < 2 accounts exist, the account pickers should not be visible
- When on the dashboard, I want to have a button to directly add a transaction. It should include an account select field so I can choose the account
- When looking at the timeline, it's very unclear to me how to use it. Right now, when I horizontally scroll, the whole gannt chart scrolls, including the fade effect. It's also super unclear what the timeline is, and what the different areas are. This needs way more work. I think it makes more sense if the blocks are shown per recurrence - so if I add -1000 for 'housing' each month, I see a block per month instead of one block that says 'ongoing'
- When looking at categories, do not use the card around 'Categories'. The title in the card is also redundant with the page title.
- When looking at categories, I want to drag and drop categories to change the structure
- When looking at categories, I want a + button to each category to easily add a sub category
- When looking at categories, show the add sub category and edit buttons only on hover to present a clean UI
- The overall design aesthetic is very basic. I want to give examples of other apps and tools to improve this - we need to brainstorm on this together before making changes!
