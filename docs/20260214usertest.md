- Bug: When looking at the app in an empty state, the total networth in the sidebar shows '£0' instead of using the locale (euro in my case). When adding 1 account, it changes to doing this correctly
- When looking at the planning page and the networth is negative, I want to clearly see it's negative (for example see the 0 line in the middle so it's clear it's under 0)
- UX on Financial goals: It's unclear where the growth is coming from, since now transactions are shown. We need to brainstorm on how to improve this - should we show transactions, or should the interest version be a fixed scenario, etc.
- When creating a liability account it's really weird that a positive number is counted as 'negative'. Make this simpler, both assets as liabilities are using absolute values -> +10.000 is postive, -10.000 is negative.
